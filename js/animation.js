// const movingElement = document.querySelector(".animation-span");

// document.addEventListener("mousemove", onMouseMoveAnimation);

// function onMouseMoveAnimation(event) {
//     movingElement.style.top = `${event.pageY}px`;
//     movingElement.style.left = `${event.pageX}px`;
// }

